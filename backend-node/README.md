# Movie Recommendation Express API

This folder contains the Node.js/Express backend that was separated from the Spring Boot project.

## Run

```bash
npm install
npm run dev
```

Copy `.env.example` to `.env` and fill in the database and TMDB values before running.
